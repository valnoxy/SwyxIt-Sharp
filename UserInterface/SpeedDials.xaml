<UserControl x:Class="SwyxSharp.UserInterface.SpeedDials"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SwyxSharp.UserInterface"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:WidthToColumnsConverter x:Key="WidthToColumnsConverter"/>
        <local:StateToColorConverter x:Key="StateToColorConverter" />
    </UserControl.Resources>

    <Grid RowDefinitions="Auto,*">
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <ui:TextBlock FontTypography="Subtitle" FontWeight="Bold" Text="Kurzwahlen" Margin="0,0,0,10"/>
            <Grid ColumnDefinitions="*,Auto,Auto,Auto,Auto">
                <ui:TextBox PlaceholderText="Suche" Icon="{ui:SymbolIcon Search24}" Grid.Column="0"/>
                <ui:Button Icon="{ui:SymbolIcon Add24}" Content="Hinzufügen" Grid.Column="1" Margin="10,0,20,0" Height="36"/>
                <ui:Button Content="Alle" Grid.Column="2" Margin="0,0,5,0" Appearance="Primary" Height="36" Width="86"/>
                <ui:Button Content="Verfügbar" Grid.Column="3" Height="36" Width="86"/>
                <ui:Button Icon="{ui:SymbolIcon Bug24}" Click="DebugLoad" Grid.Column="4" Margin="10,0,0,0" Height="36" Appearance="Danger"/>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Cards}" 
                          HorizontalContentAlignment="Stretch"
                          VerticalAlignment="Top"
                          Margin="-4,0,0,0"
                          x:Name="CardsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid VerticalAlignment="Top"
                                     Columns="{Binding ActualWidth, ElementName=CardsControl, Converter={StaticResource WidthToColumnsConverter}}" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Padding="12" Margin="4">
                            <Grid ColumnDefinitions="Auto,Auto,*" Height="60">
                                <Grid Grid.Column="0" 
                                      Width="60" 
                                      Height="60" 
                                      Margin="0,0,10,0" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center">

                                    <Ellipse Width="50" Height="50"
                                             HorizontalAlignment="Left" 
                                             VerticalAlignment="Top">
                                        <Ellipse.Fill>
                                            <ImageBrush ImageSource="{Binding Picture}" Stretch="UniformToFill" />
                                        </Ellipse.Fill>
                                    </Ellipse>

                                    <Ellipse Width="15" Height="15" 
                                             Fill="{Binding State, Converter={StaticResource StateToColorConverter}}" 
                                             Stroke="White" 
                                             StrokeThickness="1"
                                             HorizontalAlignment="Left" 
                                             VerticalAlignment="Top"
                                             Margin="36,36,0,0"/>
                                </Grid>
                                <StackPanel Grid.Column="1">
                                    <ui:TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16"/>
                                    <TextBlock Text="{Binding Status}"/>
                                </StackPanel>
                                <ui:Button Grid.Column="2" Icon="{ui:SymbolIcon Symbol=MoreVertical24}" Padding="0" 
                                           FontSize="18" VerticalAlignment="Top" HorizontalAlignment="Right"/>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
